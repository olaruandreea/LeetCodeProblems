import math  

class Solution(object):
    def mySqrt(self, x):
        sqrt = math.sqrt(x)
	if "." in str(sqrt):
		tokens = str(sqrt).split(".")
		return tokens[0]
	else:
		return sqrt
test = Solution()
print test.mySqrt(8)
        
